//Crie um programa que dado um número, imprima a sua tabuáda.

let i = 1;
let resultado

for (let i = 0; i <= 10; i++) {
    const numero = 5;
    resultado = numero * i;
    console.log(numero + ' x ' + i + ' = ' + resultado);
}
